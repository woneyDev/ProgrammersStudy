class Solution {
    public int solution(int a, int b, int n) {
        int answer = 0;
        int free = 0;
        while ( n >= a ) {
            n += b - a;
            answer += b;
        }
        return answer;
    }
}
