class Solution {
    public int[] solution(int start, int end) {
        int size = end - start;
        int cnt = 0;
        int[] answer = new int[size + 1];

        while ( start <= end ) {
            answer[cnt] = start;
            cnt ++;
            start ++;
        }
        return answer;
    }
}
