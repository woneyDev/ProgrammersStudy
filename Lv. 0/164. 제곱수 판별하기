class Solution {
    public int solution(int n) {
        int answer = 0;
        int tmp = (int)Math.sqrt(n);
        if ( tmp * tmp == n ) {
            answer = 1;
        } else {
            answer = 2;
        }
        return answer;
    }
}
