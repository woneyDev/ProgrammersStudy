class Solution {
    public int[] solution(int start, int end) {
        int[] answer = new int[start - end + 1];
        int cnt = 0;

        while ( start >= end ) {
            answer[cnt] = start;
            start --;
            cnt ++;
        }
        return answer;
    }
}
