class Solution {
    public String[] solution(String my_string) {
        String[] answer = new String[my_string.split(" ").length];

        for ( int i = 0; i < answer.length; i ++ ) {
            answer[i] = String.valueOf(my_string.split(" ")[i]);
        }

        return answer;
    }
}
